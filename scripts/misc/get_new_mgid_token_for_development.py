from config.config import *
import os
import json
import requests


# I would like to go through ssh into the server, grab the newest token, and
# then put it in my development environment. 

# for now I will just manually do that. 

